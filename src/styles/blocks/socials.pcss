.socials {
  display: flex;
}
.socials__icon {
  width: 33px;
  height: 33px;
  fill: rgba(255, 255, 255, 0.3);
  padding-left: 9px;
  padding-top: 9px;

  &:hover {
  fill: #ff9a00;
  transition: 1s;
}
}
.socials__item {
  margin-right: 30px;

  &:last-child {
    margin-right: 0;
  }
}